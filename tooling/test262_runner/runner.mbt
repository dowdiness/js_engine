///|
/// Native-only MoonBit shadow of `scripts/test262-runner.py`.
///
/// Python remains authoritative. The implementation is split by concern across
/// this package; `cmd/test262_runner` is a shadow command and does not replace
/// `scripts/test262-runner.py`.
