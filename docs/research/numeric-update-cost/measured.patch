diff --git a/compiler/bytecode_frame_transition.mbt b/compiler/bytecode_frame_transition.mbt
index 36f1af8c..bc0b9c6d 100644
--- a/compiler/bytecode_frame_transition.mbt
+++ b/compiler/bytecode_frame_transition.mbt
@@ -32,18 +32,27 @@ fn suspend_bytecode(
 }
 
 ///|
-fn reduce_bytecode_local_update(
-  slot : Int,
+fn plan_bytecode_local_update(
   op : @ast.UpdateOp,
   prefix : Bool,
   converted : Value,
-) -> BytecodeActivationResumeDecision raise Error {
+) -> @runtime.NumericUpdatePlan raise Error {
   guard converted is Number(old_number) else {
     raise @errors.InternalError(
       message="numeric conversion completed without a Number",
     )
   }
-  let plan = @runtime.plan_numeric_update(old_number, op, prefix)
+  @runtime.plan_numeric_update(old_number, op, prefix)
+}
+
+///|
+fn reduce_bytecode_local_update(
+  slot : Int,
+  op : @ast.UpdateOp,
+  prefix : Bool,
+  converted : Value,
+) -> BytecodeActivationResumeDecision raise Error {
+  let plan = plan_bytecode_local_update(op, prefix, converted)
   BytecodeResumeUpdateLocal(
     slot,
     plan.assigned_value(),
diff --git a/compiler/bytecode_vm.mbt b/compiler/bytecode_vm.mbt
index 1fe73efc..89ab9e6c 100644
--- a/compiler/bytecode_vm.mbt
+++ b/compiler/bytecode_vm.mbt
@@ -1182,8 +1182,11 @@ fn BytecodeFrame::step_bytecode(
         match
           @runtime.begin_executor_numeric_conversion(interp, locals[slot], loc) {
           @runtime.ExecutorNumericConversionCompleted(value) => {
-            let decision = reduce_bytecode_local_update(slot, op, prefix, value)
-            frame.apply_activation_resume_decision(decision)
+            // A synchronous update needs no general activation-resume decision.
+            // Both paths use the same validated runtime-owned numeric plan.
+            let plan = plan_bytecode_local_update(op, prefix, value)
+            locals[slot] = plan.assigned_value()
+            stack.push(plan.expression_value())
           }
           @runtime.ExecutorNumericConversionSuspended(request) =>
             return suspend_bytecode(
