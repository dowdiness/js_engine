///|
pub fn setup_builtins(env : Environment, output : Array[String]) -> Unit {
  // console.log is handled via special-casing in interpreter
  // This function is reserved for future builtins
  let _ = env
  let _ = output

}
