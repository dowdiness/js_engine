{
  "interpreter/runtime/host_slots.mbt:host_slot_id_counter": "Process/package-instance allocator for HostSlotKey::reserve (#518). Embedder slot identities are intentionally shared across interpreters in one loaded runtime package (approved design: not per-realm/per-interpreter). Monotonic Int; abort on exhaustion, never wrap. Tests isolate by reserving fresh keys per case rather than resetting the counter.",
  "interpreter/stdlib/unicode_case_folding.mbt:unicode_simple_fold_inverse": "Generated Unicode 17.0.0 simple-fold inverse lookup table for #576. MoonBit represents the fixed literal as Array, but the private binding is never returned or mutated; runtime code only reads length and indexed pairs.",
  "interpreter/stdlib/unicode_case_folding.mbt:unicode_simple_fold_mappings": "Generated Unicode 17.0.0 simple-fold forward lookup table for #576. MoonBit represents the fixed literal as Array, but the private binding is never returned or mutated; runtime code only reads length and indexed pairs."
}
